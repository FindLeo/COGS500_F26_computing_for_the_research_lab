# Utah Git presentation checkpoints selected before conversion

Source: `inputs/git_slides.pptx`, 66 slides. Codex inspected the original PowerPoint text and rendered slides 20 and 53 on 2026-09-22 before seeing the MarkItDown output.

## Five factual details

1. Slide 5: Git is version-control software for tracking versions of files.
2. Slide 8: Git and GitHub are different; GitHub is a web platform for storing, sharing, and working on Git repositories.
3. Slide 20: Only changes in the staging area are included in a commit, after changes move from the working tree to the staging area.
4. Slide 40: `git switch -c new_branch_name` creates and switches to a new branch.
5. Slide 63: A README tells prospective users and contributors about the project and is often written in Markdown.

## Three structural features

1. Slide boundaries and slide titles remain identifiable across the selected slides.
2. Slide 20's working-tree → staging-area → local-repository diagram preserves its order and the `add` and `commit` transitions, either directly or via equivalent explanatory text.
3. The command examples on slide 27 remain distinguishable from surrounding prose, and the image-only remote-repository examples on slide 53 remain available or are explicitly reported as omitted. Record these as separate observations.

Describe preservation, alteration, or loss without calculating a score. The historical source deck is a conversion benchmark, not necessarily current Git guidance.
